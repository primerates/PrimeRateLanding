import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const VendorsTab = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Vendors Information</CardTitle>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Vendors functionality will be implemented here.</p>
            </CardContent>
        </Card>
    );
};

export default VendorsTab;